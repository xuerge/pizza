package ${package}.${artifactId}.base;

import ${package}.${artifactId}.TestApplication;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = TestApplication.class)
public class BaseTest{
}
